<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Only the updates folder, and only from the cache: the installer
         needs to read the APK this app just downloaded, and nothing else. -->
    <cache-path name="updates" path="updates/" />
</paths>
