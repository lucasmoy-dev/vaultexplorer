<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Dark mode: same idea, the platform's dark variant. Compose paints
         the actual UI either way. -->
    <style name="Theme.LiveSubs" parent="@android:style/Theme.DeviceDefault.NoActionBar" />
</resources>
