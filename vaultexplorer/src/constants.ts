export const DEFAULT_START_KEY = "vaultexplorer:default-start-path";
// Android's shared storage root (Download/Pictures/DCIM/Documents live as
// its subfolders) -- browsing it needs the "All files access" special
// permission, requested on tap if not already granted.
export const PHONE_STORAGE_PATH = "/storage/emulated/0";
